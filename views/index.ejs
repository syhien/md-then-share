<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MD then Share</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1>Share Markdown</h1>
        <form action="/upload" method="post" enctype="multipart/form-data">
            <div>
                <h3>Upload a .md file</h3>
                <input type="file" name="mdfile" accept=".md,.markdown">
            </div>
            <hr>
            <div>
                <h3>Or paste your markdown here</h3>
                <textarea name="mdtext" rows="10"></textarea>
            </div>
            <button type="submit" class="btn">Generate Link</button>
        </form>
        <% if (locals.error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
        <% if (locals.url) { %>
            <div>
                <h2>Share this link:</h2>
                <p>
                    <a href="<%= url %>"><%= url %></a>
                </p>
            </div>
        <% } %>
    </div>
</body>
</html>
